##joist:"0.1.0"

// Sample Joist file to unit test the lexer

config {
  projectName: string
  projectId: number
  buildType: enum[
    'a'
    'b'
    'c'
  ]
}

stage('Stage 1', always) {
  sh('echo "bla 15"')
}
