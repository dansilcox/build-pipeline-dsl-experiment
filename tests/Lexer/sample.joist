##joist:"1.3.5"

// Sample Joist file to unit test the lexer

config {
  projectName: string
  projectId: number
  buildType: enum[
    'a'
    'b'
    'c'
  ]
}

stage('Stage 1', always) {
  sh('echo "bla 15"')
}

// stage('Build', (action == 'build')) {
//  sh('bin/build.sh')
// }
